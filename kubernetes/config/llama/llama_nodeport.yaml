#Nodeport for testing
apiVersion: v1
kind: Service
metadata:
  name: llama-nodeport
spec:
  type: NodePort
  selector:
    name: llama-server
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080