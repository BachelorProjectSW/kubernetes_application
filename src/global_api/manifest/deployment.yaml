apiVersion: apps/v1
kind: Deployment
metadata:
  name: global-api

spec:
  replicas: 1

  strategy:
    type: Recreate


  selector:
    matchLabels:
      app: global-api

  template:
    metadata:
      labels:
        app: global-api

    spec:

      nodeSelector:
        node-role.kubernetes.io/control-plane: "true"

      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"

      containers:
        - name: global-api
          image: ghcr.io/bachelorprojectsw/global-api:cd04ce8c46cbbd9f32e26ce76016b0f7a2c407a3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8020
              hostPort: 8020
              protocol: TCP
              
          env:                                           
            - name: ELECTRICITY_MAPS_API_KEY              
              valueFrom:                                  
                secretKeyRef:                             
                  name: global-api-secrets                
                  key: ELECTRICITY_MAPS_API_KEY