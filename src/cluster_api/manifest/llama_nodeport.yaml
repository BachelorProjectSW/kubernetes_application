apiVersion: v1
kind: Service
metadata:
  name: llama-service
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    name: llama-server
  ports:
    - port: 8080
      targetPort: 8080