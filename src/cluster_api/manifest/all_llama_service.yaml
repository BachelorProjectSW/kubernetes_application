apiVersion: v1
kind: Service
metadata:
  name: llama-service
spec:
  selector:
    app: llama-server
  ports:
    - port: 8080 #listens on port 8080
      targetPort: 8080 #forwards traffic to any llama pod on port 8080 (daemonset)
  type: ClusterIP