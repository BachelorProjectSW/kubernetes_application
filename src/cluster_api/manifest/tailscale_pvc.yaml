#Keeps the same Tailscale machine identity across pod restarts
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cluster-api-tailscale-state
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi